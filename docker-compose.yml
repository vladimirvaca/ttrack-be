services:
  db:
    image: postgres:15.3-alpine
    restart: always
    environment:
      - POSTGRES_DB=ttrack-db
      - POSTGRES_USER=ttrack-user
      - POSTGRES_PASSWORD=ttrack-password
    ports:
      - '5432:5432'
    volumes:
      - ../ttrack-db:/var/lib/postgresql/data
