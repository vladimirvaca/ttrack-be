# Environment variables for Docker Compose deployment
# Copy this file to .env and adjust values for your environment

# Database Configuration
POSTGRES_DB=ttrack-db
POSTGRES_USER=ttrack-user
POSTGRES_PASSWORD=change-this-password-in-production
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=8080

# Docker Image (from GitLab Container Registry)
# Format: registry.gitlab.com/your-username/ttrack-be:tag
DOCKER_IMAGE=registry.gitlab.com/your-username/ttrack-be:latest

# JWT Configuration (IMPORTANT: Change these in production!)
JWT_SECRET=your-secure-jwt-secret-key-minimum-256-bits
JWT_ACCESS_TOKEN_EXPIRATION=3600
JWT_REFRESH_TOKEN_EXPIRATION=86400
